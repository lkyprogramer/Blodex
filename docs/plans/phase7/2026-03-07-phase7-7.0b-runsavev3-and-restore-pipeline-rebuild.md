> 执行前必须先完整阅读并接受：`docs/plans/phase7/2026-03-07-phase7-technical-debt-first-refactor-roadmap.md`

# Phase 7 - 7.0B RunSaveV3 与 Restore Pipeline 重建

**阶段**: `7.0B`  
**优先级**: `P0`  
**对应问题**: save/resume 状态层次混乱、timeline rebasing 脆弱、旧兼容路径与 fallback 逻辑污染结构

---

## 1. 阶段目标

用全新的严格 schema 和恢复流水线替换现有 save/resume 拼接式实现，彻底消化 Phase 6 期间暴露出来的恢复脆弱性。

完成标准：

1. 引入全新 `RunSaveV3` 严格 schema。
2. restore 只允许经过 `strict deserialize -> load -> derived rebuild -> ephemeral bootstrap`。
3. 删除旧 save key、旧 normalize helper、旧 fallback deserialize。
4. compare queue、prompt state、buff timeline、reward transient state 全部显式归类。
5. 对旧 save key 不做 silent wipe，而是给出一次性提示后清理。

---

## 2. 当前问题

1. save/resume 现在混杂了 persistent state、derived state、session-local state、UI transient state。
2. 过去多个阶段都在持续修 restore 问题，说明模型边界不稳定。
3. timeline 字段（`nowMs / expiresAtMs / appliedAtMs / queue pointer`）没有统一分类。
4. 旧兼容路径会让重构后的结构继续背负历史条件分支。

---

## 3. 范围与非目标

### 3.1 范围

1. `RunSaveV3`
2. serializer / deserializer
3. restore pipeline
4. derived rebuild / ephemeral bootstrap
5. save key 策略与用户提示

### 3.2 非目标

1. 不保留旧 save 兼容。
2. 不做旧数据迁移。
3. 不在本阶段扩玩法系统。
4. 不新增美术/音频资源；旧存档重置提示必须复用现有 UI/toast 能力。

---

## 4. 技术方案

### 4.1 新状态分层

强制拆成四层：

1. `Persistent Domain State`
   - run/player/equipment/inventory/currency/route/mutation
2. `Persistent Runtime Snapshot`
   - floor progression、boss runtime core state、event node core state、spike budget、telemetry aggregates
3. `Restore-Derived State`
   - buff-derived player stats
   - monster derived moveSpeed / damage profile
   - compare candidate baselines
4. `Ephemeral Session State`
   - overlay lock
   - compare queue pointer
   - active prompt context
   - session-local timers

### 4.2 `RunSaveV3` 规则

1. 新 schema 严格定义每个字段归属。
2. 不保留 `V1/V2` 类型与 helper。
3. 所有旧 key 视为过期格式。

### 4.3 Restore Pipeline

固定顺序：

1. strict deserialize
2. load persistent data into state containers
3. run derived rebuilders
4. bootstrap ephemeral runtime
5. hand control back to `DungeonSessionFacade`

禁止：

1. 在 deserialize 阶段直接 mutate Scene
2. 在 derived rebuild 中读取 UI transient state
3. 用 fallback 魔法值重建运行时

### 4.4 Timeline / Buff / Queue 规则

1. runtime local 时间字段只允许保存在会话态或以剩余时长形式保存。
2. buff 只保存剩余时长或稳定定义，不保存跨 session 的绝对 runtime 时间。
3. compare / reward / prompt queue 必须有显式 snapshot 与 restore 语义。

### 4.5 旧 key 清理策略

1. 若检测到旧 save key 存在且 `RunSaveV3` 不存在：
   - 显示一次性提示：`存档格式已重置，旧进度不可继续`
   - 记录一次 release-note style log
   - 清理旧 key
2. 不尝试迁移旧数据。

### 4.6 资源策略

1. 本阶段不新增 UI 贴图、图标、音频 cue。
2. “存档格式已重置，旧进度不可继续” 必须复用现有 toast / modal 样式与现有音频体系。
3. 不允许为了 save 重构单独引入新的提示资产。

---

## 5. PR 拆分

### PR-7.0B-01 RunSaveV3 Strict Schema

1. 定义新 schema
2. serializer / deserializer
3. 删除旧 save key 与旧 migration 入口

### PR-7.0B-02 Restore Pipeline

1. load stage
2. derived rebuild stage
3. ephemeral bootstrap stage
4. timeline rebasing 统一化

### PR-7.0B-03 Prompt / Queue / Buff Session Model

1. compare queue snapshot
2. reward queue snapshot
3. prompt state snapshot
4. buff timeline snapshot

---

## 6. 验证方案

### 6.1 自动化

```bash
pnpm --filter @blodex/core test
pnpm --filter @blodex/game-client test
pnpm ci:check
```

新增重点：

1. `save-restore-pipeline.contract.test`
2. `runsavev3.schema.test`
3. `derived-state-rebuilder.test`
4. `ephemeral-bootstrap.test`
5. `old-save-key-reset-notice.test`

### 6.2 手动冒烟

1. 保存 -> 刷新 -> 恢复完整 run
2. event panel / compare / boss reward / summary 组合恢复
3. active buff / debuff / boss floor / merchant open 时恢复
4. 检查旧 key 存在时是否有一次性提示

---

## 7. 出口门禁

1. save/restore 全量切换到 `RunSaveV3`。
2. 不存在旧 migration / fallback deserialize 残留。
3. 所有 restore 路径都经过统一 pipeline。
4. 不存在 silent wipe。

---

## 8. 风险与止损

1. 风险：重建 schema 时遗漏 transient 状态。  
   止损：先列完整状态清单，再实现 serializer。
2. 风险：derived rebuild 顺序错误导致恢复后属性异常。  
   止损：所有 rebuild stage 独立测试，不允许隐式顺序。
3. 风险：完全破坏式修改影响玩测节奏。  
   止损：用一次性提示 + release note 减少困惑，但不做旧数据迁移。

---

## 9. 与 7.0C 交接

进入 `7.0C` 前必须确认：

1. save/resume 已不再是 release evidence 的高风险回归点。
2. 所有与录像/复盘相关的 run snapshot 已有稳定 schema。

---

## 10. 当前实现结果（已落地）

截至本轮开发，`7.0B` 已按破坏式重构方向落地，当前实现状态如下：

1. 已引入严格 `RunSaveV3`，保存结构固定为：
   - `domain`
   - `runtime`
   - `session`
2. 已删除旧 `V1/V2` 兼容链、migration helper、normalize/fallback deserialize。
3. 已实现固定 restore pipeline：
   - `strict deserialize`
   - `load persistent state`
   - `rebuild derived state`
   - `bootstrap ephemeral session`
4. buff 已按 `remainingMs` 持久化，不再跨 session 保存绝对 runtime 时间。
5. compare prompt 已重建为显式 session state：
   - `active`
   - `immediate`
   - `deferred`
6. compare prompt 在以下场景已闭环：
   - prompt 打开期间自动保存
   - 刷新后继续挑战
   - 恢复后重新弹出同一条 prompt
7. 已实现旧 save key 的破坏式清理与一次性提示：
   - 检测旧 key
   - 清理旧 key
   - 显示“存档格式已重置，旧进度不可继续”
8. `RunSaveV3` 的 strict schema 已补齐 persistent player/monster validator，不再接受缺少核心字段的损坏 save。

---

## 11. 本阶段实际验证

### 11.1 自动化验证

本阶段已实际通过：

```bash
pnpm --filter @blodex/core typecheck
pnpm --filter @blodex/game-client typecheck
pnpm --filter @blodex/core exec vitest run src/__tests__/save.test.ts
pnpm --filter @blodex/game-client exec vitest run src/scenes/dungeon/feedback/__tests__/HeartbeatFeedbackRuntime.test.ts src/scenes/dungeon/save/__tests__/runStateRestorer.test.ts src/scenes/dungeon/save/__tests__/runSaveSnapshotBuilder.test.ts
pnpm ci:check
```

重点覆盖：

1. `RunSaveV3` schema round-trip
2. malformed player/monster save 拒绝
3. compare prompt `active/immediate/deferred` snapshot/restore
4. restore 后 compare prompt 自动重开
5. blocked restore 后经 flush 再展示

### 11.2 浏览器烟测

已使用 Chrome DevTools + 金手指完成快速实机验证，结论为主流程正常：

1. 从 Meta 开始新挑战，能正常进入地牢。
2. 地牢内能正常写出 `blodex_run_save_v3`。
3. 刷新后返回 Meta，能出现“继续挑战”。
4. 点击“继续挑战”后，能正常恢复进入地牢。
5. compare prompt 打开期间刷新，恢复后会重新显示同一条 prompt。
6. 手工注入旧 `blodex_run_save_v2` 后，刷新页面会显示一次性重置提示，并清除旧 key。
7. 浏览器控制台未出现新的 runtime error。

---

## 12. 后续窄兼容例外备案

`7.0B` 的原则仍然是：

1. 不保留 `v1 / v2` save 兼容
2. 不恢复旧 migration helper
3. 不恢复旧 fallback deserialize

但主干后续在 `7.8` 引入了一个**窄兼容例外**，必须正式备案：

1. 当 `RunSaveV3` 的 `powerSpikeBudgetState.pairStates` 仍是旧布局：
   - `1-2`
   - `3-4`
   - `5`
2. 反序列化入口会在严格校验前，把旧的 `pairStates["5"]` 归一化成：
   - `5-6`
   - `7-8`

该例外的边界是：

1. 只适用于 `RunSaveV3` 内部 reward-curve 布局扩展
2. 不接住 `v1 / v2`
3. 不恢复旧 schema 的宽松接受
4. 仍然保持 `strict deserialize -> load -> rebuild -> bootstrap` 的主模型不变

---

## 12. 当前出口判断

当前 `7.0B` 可以判定为：

1. `Development Complete`
2. `Automation Pass`
3. `Browser Smoke Pass`

当前未纳入本阶段目标的事项：

1. 不提供旧 save 数据迁移能力。
2. 不保证旧 key 兼容恢复。
3. 不扩展新的玩法系统或内容资源。
