> 执行前必须先完整阅读并接受：`docs/plans/phase7/2026-03-07-phase7-technical-debt-first-refactor-roadmap.md`

# Phase 7 - 7.6 Affix、Consumable、Merchant 与 Talent 扩充

**阶段**: `7.6`  
**优先级**: `P2`  
**前置条件**: `7.3` gate 已通过  
**对应问题**: 中层内容密度不足，run 差异性依然主要靠装备与路线分歧
**当前状态**: `Development Complete / Automation Pass`

---

## 1. 阶段目标

扩充中层内容密度，让普通房间、商店、消耗品和天赋投入形成更高频的 replayability。

完成标准：

1. monster affix 扩到 `8+`
2. consumable 内容池正式激活
3. merchant 从纯随机事件增强为更稳定遭遇
4. talent 从开/关两态扩到更有投入感的 rank 模型

---

## 2. 当前问题

1. 普通战斗中层差异还不够强。
2. consumable 类型存在，但内容密度不足。
3. merchant 稳定性不足。
4. talent 投入感不强。

---

## 3. 范围与非目标

### 3.1 范围

1. affix 批次一
2. consumable enablement
3. merchant stabilization
4. talent rank expansion

### 3.2 非目标

1. 不在本阶段扩完整元素体系。
2. 不在本阶段扩长 run。

---

## 4. 技术方案

### 4.1 Monster Affix Batch One

原则：

1. 优先行为差异，不优先数值词缀。
2. affix 必须有可感知行为与反馈。

### 4.2 Consumable Enablement

原则：

1. 每类 consumable 必须有明确用途与 UI 表达。
2. 不做无意义的平铺药水池。

### 4.3 Merchant Stabilization

方向：

1. 提高商店的稳定出现概率或独立房间化。
2. 商店 compare / value gating 继续复用现有 compare 系统。

### 4.4 Talent Rank Expansion

方向：

1. 从 binary unlock 改成 rank model
2. rank 带来明确成长曲线与选择压力

### 4.5 美术与音频方案

#### Monster Affix

1. 默认优先复用现有怪物底图，通过：
   - tint
   - overlay glyph
   - telegraph variation
   - 小型 VFX anchor
   表达 affix 身份
2. 只有行为差异无法靠现有层表达时，才新增专属 affix overlay 资产。
3. 风格必须遵循 [art-style-bible.md](/Users/luo/Documents/github/Blodex/docs/art-style-bible.md)，禁止高饱和状态特效。

#### Consumable

1. 最小视觉资源集：
   - `item_consumable_health_*`
   - `item_consumable_mana_*`
   - `item_consumable_scroll_*`
   - `item_consumable_elixir_*`
2. icon 必须保持 painterly dark fantasy 的单体可读性，背景透明，避免现代药瓶或高亮玻璃质感。

#### Merchant

1. 若升级为独立节点/房间，最小视觉资源集：
   - `merchant_node_marker_01`
   - `merchant_room_sign_01`
   - `merchant_portrait_01`（可选）
2. 音频最小增量：
   - `ui_merchant_special_open_01`
   - `ui_merchant_special_buy_01`

#### Talent

1. rank 扩展优先复用现有 icon 系统。
2. 只有当 rank 表达无法靠文本/数字解决时，再补充：
   - `ui_talent_rank_pip_01`
   - `ui_talent_rank_glow_01`

#### 资产治理

1. affix / consumable / merchant / talent 的资源必须分批写入 `asset-plan` 与 `audio-plan`。
2. 每一批都先定义最小上线集，再定义可选增强集。
3. 任何新增 icon / marker / portrait / overlay 的实际美术生成，都必须等待用户提供 Gemini Key。
4. 在 Gemini Key 未提供前，本阶段只允许完成资源清单、prompt、命名和占位接线，不允许直接运行图片生成。

### 4.6 已落地结果

本阶段已实际完成：

1. Monster affix 从 `4` 扩到 `8`：
   - 既有：`armored / vampiric / splitting / frenzied`
   - 新增：`hulking / warded / skirmisher / manaburn`
2. `consumable` 正式启用 3 个新内容：
   - `scroll_of_mapping_plus`
   - `frenzy_tonic`
   - `phantom_brew`
   - 高阶 consumable 的事件来源现在优先播种 blueprint / recipe progress，只有在对应 blueprint 已锻造后才会直接发 charges
3. Merchant 稳定出现改成：
   - `floor 2 / 4` 保底商人事件
   - `merchantDiscount` talent 进入真实定价链
4. Talent rank 扩展为真实多级模型：
   - `core_vitality_training` 提升到 `3`
   - 多个 `core / class / utility` talent 提升到 `2`
5. 新增内容已接入：
   - runtime
   - i18n
   - log / feedback
   - compare / HUD
   - unlock / integrity checks
6. 本阶段未生成新的美术资源，也未请求 Gemini Key；仍复用现有 placeholder/icon 体系。

---

## 5. PR 拆分

### PR-7.6-01 affix batch one

1. `4 -> 8+` affix
2. 行为与反馈接线

### PR-7.6-02 consumable enablement

1. consumable defs
2. UI / inventory / runtime use-path

### PR-7.6-03 merchant stabilization

1. stable spawn / room
2. compare / offer policy

### PR-7.6-04 talent rank expansion

1. rank model
2. meta/runtime sync

---

## 6. 验证方案

1. affix 行为差异 run
2. consumable 使用 run
3. merchant 稳定节点 run
4. talent rank 对照 run

### 6.1 实际验证结果

已实际通过：

1. `pnpm --filter @blodex/core typecheck`
2. `pnpm --filter @blodex/content typecheck`
3. `pnpm --filter @blodex/game-client typecheck`
4. `pnpm --filter @blodex/core exec vitest run src/__tests__/monsterAffix.test.ts src/__tests__/combat.contract.test.ts src/__tests__/consumable.test.ts src/__tests__/talent.test.ts src/__tests__/save.test.ts`
5. `pnpm --filter @blodex/content exec vitest run src/__tests__/bossRegistry.test.ts src/__tests__/integrity.test.ts src/__tests__/ids-uniqueness.test.ts`
6. `pnpm --filter @blodex/game-client exec vitest run src/scenes/dungeon/encounter/__tests__/playerActionModule.test.ts src/scenes/dungeon/world/__tests__/eventRuntimeModule.test.ts src/scenes/dungeon/world/__tests__/eventResolutionService.test.ts src/i18n/__tests__/labelResolvers.test.ts`
7. `pnpm --filter @blodex/game-client i18n:check`
8. `pnpm ci:check`

浏览器白盒烟测已确认：

1. Meta -> `继续挑战` 可正常进入地牢，主流程无 runtime error。
2. `openMerchant()` 可正常打开流浪商人面板，购买低价值物品后：
   - 欧铂正确扣减
   - 背包与日志正确刷新
   - compare / HUD 不崩溃
3. `grantConsumables()` 后，HUD 会正常展示新增的：
   - `scroll_of_mapping_plus`
   - `frenzy_tonic`
   - `phantom_brew`
4. 实际使用 `frenzy_tonic` 后：
   - 玩家攻击力立即提升
   - buff 观察日志出现
   - consumable 进入冷却并正确显示剩余充能

---

## 7. 出口门禁

1. 中层内容差异显著提升。
2. consumable / merchant / talent 都形成稳定系统，而不是试验性内容。

### 7.1 出口判断

当前阶段可以视为已完成，依据如下：

1. 新 affix 已形成行为差异，而非纯数值词缀。
2. consumable 已有真实获得、快捷键使用、buff/runtime/log/i18n 主链。
3. merchant 不再完全依赖随机命中，`floor 2 / 4` 已具备稳定节点。
4. talent 已从开/关两态扩展到多级投入模型，并同步 legacy mapping。
5. 自动化验证与 `ci:check` 已通过。

---

## 8. 风险与止损

1. 风险：内容量增加但系统接线不稳定。  
   止损：每一类都先在既有 runtime/UI 边界上接入，不开新临时入口。
2. 风险：多级 talent 膨胀过快，影响后续数值治理。  
   止损：`7.2` 的 calibration / governance 主链继续作为后续调优入口，不在内容阶段回退成散落常量。
3. 风险：资源占位长期停留，导致后续阶段误判为“资源已完成”。  
   止损：本阶段明确未生成新美术；后续若进入 boss / set / 元素资源生产，必须在提供 Gemini Key 后单独执行。

---

## 9. 与 7.7 交接

进入 `7.7` 前必须确认：

1. affix / consumable / merchant / talent 没有重新引入 God object 回流。
2. 新增 affix 与 consumable 的语义已稳定，不再在 `7.7` 顺手改名或改用途。
3. 若 `7.7` 需要新增 icon / portrait / overlay，美术生成仍需等待用户提供 Gemini Key。
