# Phase 6 - 6.0 观测闭环收口记录

**日期**: 2026-03-06  
**执行基线**: `origin/main@e4f5464`  
**执行分支**: `codex/phase6-6.0-baseline-observability`

---

## 1. 直接结论

Phase 6.0 的主目标已经收敛为三类实际交付：

1. 把 Phase 6 关键体验项冻结成统一 telemetry 契约，并接入 run log / summary / save-resume。
2. 把 `RealBalanceSimulator` 从“纯普攻近似”升级为“带最小技能循环”的节奏观测工具。
3. 把 `scenes/dungeon/*` 主链上的 `any host` 收口成显式 port，降低 6.1 继续扩 save / telemetry / runtime rule 的架构风险。

6.0 当前不再缺“有没有数据”；  
剩下的问题已经收缩为 6.1 和 6.3 的系统补完与反馈表达。

---

## 2. 实际交付清单

### 2.1 事件字典与 telemetry schema

已落地事件：

1. `player_facing_choice`
2. `power_spike`
3. `build_formed`
4. `rare_drop_presented`
5. `boss_reward_closed`
6. `combat_rhythm_window`
7. `synergy_activated`

已落地统计面：

1. `RunSummary.phase6Telemetry`
2. run log 的 baseline / heartbeat 文案
3. save-resume 的 `Phase6TelemetryRuntimeState`
4. story / combat / runtimeEffects 三段式 snapshot

### 2.2 Story / Combat / Runtime Effect 基线

当前已可稳定导出的基线包括：

1. 每层 `choice` 次数
2. 每局 `power spike` 次数
3. `build formed` 次数
4. `rare drop presented` / `boss reward closed`
5. `skill casts / 30s`
6. `auto attack damage share`
7. `average / max no-input gap`
8. `mana dry window`
9. `buff apply count / intended uptime`
10. `damage dealt / taken / resolved hit count by damageType`
11. `synergy activation count / first floor`

### 2.3 Save / Resume 与 source taxonomy

已补齐：

1. telemetry runtime state 持久化
2. save round-trip 校验
3. restore 时 `synergy` 不重复记账
4. `merchant / event reward` 纳入 `power_spike` taxonomy

### 2.4 Simulator 与 drift gate

已补齐：

1. `RealBalanceSimulator` 最小技能循环
2. `combatRhythm` 指标输出
3. `clearRate` 全局阈值恢复到 `0.62`
4. 仅对 `hard-average` 维持 `0.68` 场景化 override
5. 报告行输出 `effectiveThresholds`

### 2.5 Host Port 收口

已完成显式 port 化的主链模块：

1. `save`
2. `world`
3. `encounter`
4. `debug`

结果：

1. `RunSaveSnapshotBuilder` / `RunStateRestorer` 不再依赖字典型 scene
2. `BossCombatService` / `BossSpawnService` / `BossTelegraphPresenter` 不再依赖裸 `any`
3. `DeferredOutcomeRuntime` / `HazardRuntimeModule` / `ProgressionRuntimeModule` 使用显式 runtime host
4. `DungeonScene` 从隐式全局容器退回到 adapter / 装配角色

---

## 3. 审计输出物

6.0 对 roadmap 中要求的审计文档已经补齐：

1. [2026-03-06-phase6-6.0-cue-asset-gap-audit.md](/Users/luo/Documents/github/Blodex-phase6-6.0/docs/plans/phase6/2026-03-06-phase6-6.0-cue-asset-gap-audit.md)
2. [2026-03-06-phase6-6.0-runtime-effect-gap-audit.md](/Users/luo/Documents/github/Blodex-phase6-6.0/docs/plans/phase6/2026-03-06-phase6-6.0-runtime-effect-gap-audit.md)

它们分别冻结了：

1. 6.3 的最小音频资源边界
2. `buff / damageType / synergy` 的真实规则缺口边界

---

## 4. 对 6.1 的交接结论

6.1 不需要再花时间回答“问题到底在不在”。  
6.0 已经把后续工作压缩成以下明确输入：

1. `BuffRuntimeClosure`
2. `DamageTypeResolverV1`
3. `combat tempo` 数值调优对比
4. `synergy / build / rare / boss reward` 的玩家反馈层

建议 6.1 继续沿用 6.0 的 baseline 口径，不要重新发明指标。

---

## 5. 提交整理建议

当前 6.0 变更适合按两个提交收口：

### Commit A

`feat(phase6): implement 6.0 telemetry baseline and typed runtime boundaries`

包含：

1. telemetry schema / summary / save-resume
2. simulator combat rhythm baseline
3. dungeon runtime typed ports
4. 相关测试

### Commit B

`docs(phase6): record 6.0 observability audits and closeout`

包含：

1. 6.0 阶段文档更新
2. cue asset gap audit
3. runtime effect gap audit
4. 6.0 closeout 记录

这样拆分的原因：

1. code commit 保持“功能 + 架构边界 + 测试”完整闭环
2. docs commit 保持“审计输出物与交接口径”独立可审阅
3. 避免因为 `DungeonScene` 的交叉改动而做高成本的交互式分块提交

---

## 6. 收口结论

Phase 6.0 已经从“规划文档”进入“可交接基线”状态。

它的价值不是让玩家立刻感觉更爽，而是先把：

1. 节奏
2. 跃迁
3. 构筑成型
4. runtime effect 缺口
5. save / restore / typed host 风险

全部转成后续阶段可以直接消费的、可量化的工程对象。
