export function contentItemNameKey(itemId: string): string {
  return `content.item.${itemId}.name`;
}

export function contentSkillNameKey(skillId: string): string {
  return `content.skill.${skillId}.name`;
}

export function contentSkillDescriptionKey(skillId: string): string {
  return `content.skill.${skillId}.description`;
}

export function contentEventNameKey(eventId: string): string {
  return `content.event.${eventId}.name`;
}

export function contentEventDescriptionKey(eventId: string): string {
  return `content.event.${eventId}.description`;
}

export function contentEventChoiceNameKey(eventId: string, choiceId: string): string {
  return `content.event.${eventId}.choice.${choiceId}.name`;
}

export function contentEventChoiceDescriptionKey(eventId: string, choiceId: string): string {
  return `content.event.${eventId}.choice.${choiceId}.description`;
}

export function contentUnlockNameKey(unlockId: string): string {
  return `content.unlock.${unlockId}.name`;
}

export function contentUnlockDescriptionKey(unlockId: string): string {
  return `content.unlock.${unlockId}.description`;
}

export function contentTalentNameKey(talentId: string): string {
  return `content.talent.${talentId}.name`;
}

export function contentTalentDescriptionKey(talentId: string): string {
  return `content.talent.${talentId}.description`;
}

export function contentBlueprintNameKey(blueprintId: string): string {
  return `content.blueprint.${blueprintId}.name`;
}

export function contentMutationNameKey(mutationId: string): string {
  return `content.mutation.${mutationId}.name`;
}

export function contentBiomeNameKey(biomeId: string): string {
  return `content.biome.${biomeId}.name`;
}

export function contentMonsterNameKey(monsterId: string): string {
  return `content.monster.${monsterId}.name`;
}

export function contentAffixNameKey(affixId: string): string {
  return `content.affix.${affixId}.name`;
}

export function contentAffixDescriptionKey(affixId: string): string {
  return `content.affix.${affixId}.description`;
}

export function contentBossNameKey(bossId: string): string {
  return `content.boss.${bossId}.name`;
}
